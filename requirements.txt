# ── Core pipeline ─────────────────────────────────────────────────────────────
pandas>=2.0,<2.3
# IMPORTANT: pinned <2.0 — the trained AutoGluon model was pickled under
# NumPy 1.x. NumPy 2.0 reorganized the internal random/BitGenerator C
# extension layout, so unpickling a NumPy-1.x RandomState/BitGenerator under
# NumPy 2.x raises:
#   ValueError: <class 'numpy.random._mt19937.MT19937'> is not a known
#   BitGenerator module.
# Keeping numpy<2.0 here (matching the training environment) avoids that.
numpy>=1.26,<2.0
scikit-learn>=1.4,<1.6
pyyaml>=6.0
mlflow>=2.12

# ── AutoGluon (core ML engine) ────────────────────────────────────────────────
# Installs AutoGluon with tabular support + all model backends
autogluon.tabular>=1.1

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.7
seaborn>=0.13

# ── EDA (optional but recommended) ────────────────────────────────────────────
ydata-profiling>=4.6
plotly>=5.18
kaleido>=0.2.1

# ── Misc ──────────────────────────────────────────────────────────────────────
scipy>=1.11
streamlit>=1.30.0

# ── Install Notes ─────────────────────────────────────────────────────────────
# Full install:
#   pip install autogluon.tabular
#
# This installs AutoGluon with: XGBoost, LightGBM, CatBoost, neural networks,
# Random Forest, GBT, and all ensemble/stacking capabilities.
#
# AutoGluon replaces the need for separate installations of xgboost,
# lightgbm, catboost for model training (though they are installed as deps).
