# ── Core pipeline ─────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scikit-learn>=1.4
pyyaml>=6.0
mlflow>=2.12

# ── AutoGluon (core ML engine) ────────────────────────────────────────────────
# Installs AutoGluon with tabular support + all model backends
# PINNED to the exact version the predictor was trained/saved with.
# AutoGluon predictors are NOT forward/backward compatible across versions —
# check_saved_predictor_version() raises AssertionError on any mismatch.
# Verify with: pip show autogluon.tabular  (or check the trained predictor's
# artifacts/models/<predictor_dir>/version.txt file)
autogluon.tabular[all]==1.5.0

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.7
seaborn>=0.13

# ── EDA (optional but recommended) ────────────────────────────────────────────
ydata-profiling>=4.6
plotly>=5.18
kaleido>=0.2.1

# ── Misc ──────────────────────────────────────────────────────────────────────
scipy>=1.11
streamlit>=1.30.0

# ── Install Notes ─────────────────────────────────────────────────────────────
# Full install:
#   pip install autogluon.tabular
#
# This installs AutoGluon with: XGBoost, LightGBM, CatBoost, neural networks,
# Random Forest, GBT, and all ensemble/stacking capabilities.
#
# AutoGluon replaces the need for separate installations of xgboost,
# lightgbm, catboost for model training (though they are installed as deps).
